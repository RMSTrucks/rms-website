---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Option 1: Split Hero | RMS Insurance" noindex={true}>

  <!-- OPTION 1: SPLIT HERO - Text left, visual element right -->
  <section class="hero">
    <div class="container hero-grid">
      <!-- Left: Content -->
      <div class="hero-content">
        <div class="hero-badge">
          <span class="badge-dot"></span>
          Serving Owner-Operators Nationwide
        </div>

        <h1>Finally,<br/><span class="gold">someone on your side.</span></h1>

        <p class="hero-sub">
          Trucking insurance from experts who actually explain things.
          No jargon. No runaround. Just answers.
        </p>

        <div class="hero-cta">
          <a href="tel:208-800-0640" class="btn">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor">
              <path d="M6.62 10.79c1.44 2.83 3.76 5.14 6.59 6.59l2.2-2.2c.27-.27.67-.36 1.02-.24 1.12.37 2.33.57 3.57.57.55 0 1 .45 1 1V20c0 .55-.45 1-1 1-9.39 0-17-7.61-17-17 0-.55.45-1 1-1h3.5c.55 0 1 .45 1 1 0 1.25.2 2.45.57 3.57.11.35.03.74-.25 1.02l-2.2 2.2z"/>
            </svg>
            Get My Quote: 208-800-0640
          </a>
          <a href="/new-authorities" class="btn btn-secondary">
            New Authority? Start Here
          </a>
        </div>
      </div>

      <!-- Right: Visual Element (abstract truck/road illustration) -->
      <div class="hero-visual">
        <div class="visual-container">
          <!-- Abstract road lines -->
          <svg class="road-illustration" viewBox="0 0 400 400" fill="none" xmlns="http://www.w3.org/2000/svg">
            <!-- Horizon glow -->
            <circle cx="200" cy="400" r="300" fill="url(#horizonGlow)"/>
            <!-- Road perspective lines -->
            <path d="M200 400 L100 150" stroke="rgba(255,255,255,0.2)" stroke-width="2"/>
            <path d="M200 400 L300 150" stroke="rgba(255,255,255,0.2)" stroke-width="2"/>
            <path d="M200 400 L150 150" stroke="rgba(255,255,255,0.1)" stroke-width="1"/>
            <path d="M200 400 L250 150" stroke="rgba(255,255,255,0.1)" stroke-width="1"/>
            <!-- Center line dashes -->
            <line x1="200" y1="380" x2="200" y2="340" stroke="#DAA520" stroke-width="4" stroke-linecap="round"/>
            <line x1="200" y1="310" x2="200" y2="270" stroke="#DAA520" stroke-width="3" stroke-linecap="round"/>
            <line x1="200" y1="240" x2="200" y2="210" stroke="#DAA520" stroke-width="2" stroke-linecap="round"/>
            <line x1="200" y1="185" x2="200" y2="165" stroke="#DAA520" stroke-width="1.5" stroke-linecap="round"/>
            <!-- Sun/destination circle -->
            <circle cx="200" cy="120" r="40" fill="url(#sunGradient)"/>
            <defs>
              <radialGradient id="horizonGlow" cx="0.5" cy="1" r="0.75">
                <stop offset="0%" stop-color="#1B4332" stop-opacity="0.5"/>
                <stop offset="100%" stop-color="transparent"/>
              </radialGradient>
              <radialGradient id="sunGradient" cx="0.5" cy="0.5" r="0.5">
                <stop offset="0%" stop-color="#DAA520"/>
                <stop offset="100%" stop-color="#C49B1A" stop-opacity="0.8"/>
              </radialGradient>
            </defs>
          </svg>

          <!-- Stats overlay -->
          <div class="stats-overlay">
            <div class="stat">
              <span class="stat-num">2,000+</span>
              <span class="stat-label">Truckers Helped</span>
            </div>
            <div class="stat">
              <span class="stat-num">$14M+</span>
              <span class="stat-label">Coverage Placed</span>
            </div>
            <div class="stat">
              <span class="stat-num">2022</span>
              <span class="stat-label">Est.</span>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Value Props -->
  <section class="values">
    <div class="container">
      <span class="section-tag">Why RMS</span>
      <h2>The Industry Profits From<br/><span class="gold">Your Confusion. We Don't.</span></h2>

      <div class="value-grid">
        <div class="value-card">
          <div class="value-num">01</div>
          <h3>We Actually Explain Things</h3>
          <p>Insurance should make sense to you. If your agent can't explain it clearly, they either don't understand it or don't want you to.</p>
        </div>

        <div class="value-card">
          <div class="value-num">02</div>
          <h3>We Know Your Business First</h3>
          <p>We ask what you haul, where you go, how you run. Then we find coverage that actually fits. Not maximum coverage. Right coverage.</p>
        </div>

        <div class="value-card">
          <div class="value-num">03</div>
          <h3>When You Call, Things Happen</h3>
          <p>You say what you need, it happens. Certificate requests get done while you're on the phone. One moment... done.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA -->
  <section class="cta">
    <div class="container">
      <h2>Ready to Talk?</h2>
      <p>Call us. We answer.</p>
      <a href="tel:208-800-0640" class="btn btn-large">208-800-0640</a>
    </div>
  </section>

</Layout>

<style>
  /* SPLIT HERO */
  .hero {
    min-height: 100vh;
    background: linear-gradient(135deg, #1B4332 0%, #0D2118 100%);
    display: flex;
    align-items: center;
    padding: 2rem 0;
  }

  .hero-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
  }

  .hero-content {
    color: white;
  }

  .hero-badge {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    background: rgba(218, 165, 32, 0.15);
    border: 1px solid rgba(218, 165, 32, 0.3);
    padding: 8px 16px;
    border-radius: 100px;
    font-size: 0.85rem;
    margin-bottom: 2rem;
  }

  .badge-dot {
    width: 8px;
    height: 8px;
    background: var(--gold);
    border-radius: 50%;
  }

  h1 {
    font-size: clamp(3rem, 5vw, 4.5rem);
    line-height: 1.1;
    margin-bottom: 1.5rem;
  }

  .gold { color: var(--gold); }

  .hero-sub {
    font-size: 1.25rem;
    opacity: 0.85;
    line-height: 1.7;
    margin-bottom: 2.5rem;
    max-width: 480px;
  }

  .hero-cta {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
  }

  .btn-secondary {
    background: transparent;
    border: 2px solid rgba(255,255,255,0.3);
  }

  .btn-secondary:hover {
    border-color: white;
    background: rgba(255,255,255,0.1);
  }

  /* Visual side */
  .hero-visual {
    position: relative;
  }

  .visual-container {
    position: relative;
    aspect-ratio: 1;
    max-width: 500px;
    margin: 0 auto;
  }

  .road-illustration {
    width: 100%;
    height: 100%;
  }

  .stats-overlay {
    position: absolute;
    bottom: 10%;
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    gap: 2rem;
    background: rgba(255,255,255,0.05);
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255,255,255,0.1);
    padding: 1.5rem 2rem;
    border-radius: 8px;
  }

  .stat {
    text-align: center;
    color: white;
  }

  .stat-num {
    display: block;
    font-family: 'Crimson Pro', serif;
    font-size: 1.75rem;
    font-weight: 600;
    color: var(--gold);
  }

  .stat-label {
    font-size: 0.75rem;
    opacity: 0.7;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  /* VALUES */
  .values {
    padding: 8rem 0;
    background: var(--bg-light);
  }

  .section-tag {
    display: block;
    font-size: 0.9rem;
    font-weight: 600;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--gold);
    margin-bottom: 1rem;
  }

  .values h2 {
    font-size: clamp(2rem, 4vw, 3rem);
    color: var(--forest);
    margin-bottom: 4rem;
  }

  .value-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2rem;
  }

  .value-card {
    background: white;
    padding: 2.5rem;
    border-radius: 8px;
    border: 1px solid var(--gray-200);
    transition: transform 0.3s ease, box-shadow 0.3s ease;
  }

  .value-card:hover {
    transform: translateY(-8px);
    box-shadow: 0 20px 40px rgba(27, 67, 50, 0.1);
  }

  .value-num {
    font-family: 'Crimson Pro', serif;
    font-size: 3rem;
    font-weight: 600;
    color: var(--gray-200);
    margin-bottom: 1rem;
  }

  .value-card h3 {
    font-family: 'Crimson Pro', serif;
    font-size: 1.4rem;
    color: var(--forest);
    margin-bottom: 1rem;
  }

  .value-card p {
    color: var(--gray-600);
    line-height: 1.7;
  }

  /* CTA */
  .cta {
    padding: 8rem 0;
    background: linear-gradient(135deg, var(--gold) 0%, #C49B1A 100%);
    text-align: center;
    color: white;
  }

  .cta h2 {
    font-size: clamp(2.5rem, 5vw, 4rem);
    margin-bottom: 0.5rem;
  }

  .cta p {
    font-size: 1.5rem;
    opacity: 0.9;
    margin-bottom: 2rem;
  }

  .btn-large {
    font-size: 1.5rem;
    padding: 20px 48px;
    background: white;
    color: var(--forest);
  }

  .btn-large:hover {
    background: var(--forest);
    color: white;
  }

  /* RESPONSIVE */
  @media (max-width: 968px) {
    .hero-grid {
      grid-template-columns: 1fr;
      gap: 3rem;
    }

    .hero-visual {
      order: -1;
    }

    .visual-container {
      max-width: 350px;
    }

    .stats-overlay {
      position: relative;
      bottom: auto;
      left: auto;
      transform: none;
      margin-top: 2rem;
      justify-content: center;
    }

    .value-grid {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 640px) {
    .hero-cta {
      flex-direction: column;
    }

    .hero-cta .btn {
      width: 100%;
      justify-content: center;
    }

    .stats-overlay {
      flex-wrap: wrap;
      gap: 1.5rem;
    }
  }
</style>
