---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Option 3: Bold Full-Bleed | RMS Insurance" noindex={true}>

  <!-- OPTION 3: BOLD FULL-BLEED - Dark, large type, dramatic -->
  <section class="hero">
    <div class="hero-bg">
      <div class="hero-gradient"></div>
      <div class="hero-grid-pattern"></div>
    </div>

    <div class="container hero-content">
      <h1>
        <span class="line line-1">Finally,</span>
        <span class="line line-2">someone on</span>
        <span class="line line-3"><span class="gold">your side.</span></span>
      </h1>

      <div class="hero-bottom">
        <p class="hero-tagline">
          Trucking insurance from experts<br/>who actually explain things.
        </p>

        <div class="hero-cta">
          <a href="tel:208-800-0640" class="btn-primary">
            <span class="btn-label">Get a Quote</span>
            <span class="btn-number">208-800-0640</span>
          </a>
        </div>

        <div class="hero-stats">
          <div class="stat">
            <span class="stat-value">2,000+</span>
            <span class="stat-name">Truckers</span>
          </div>
          <div class="stat">
            <span class="stat-value">$14M+</span>
            <span class="stat-name">Placed</span>
          </div>
          <div class="stat">
            <span class="stat-value">2022</span>
            <span class="stat-name">Est.</span>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Dark value props -->
  <section class="values">
    <div class="container">
      <div class="values-header">
        <span class="section-num">01</span>
        <h2>The Industry Profits From Your Confusion.<br/><span class="gold">We Don't.</span></h2>
      </div>

      <div class="value-columns">
        <div class="value-col">
          <h3>We Actually Explain Things</h3>
          <p>Insurance should make sense to you. If your agent can't explain it clearly, they either don't understand it or don't want you to.</p>
        </div>

        <div class="value-col">
          <h3>We Know Your Business First</h3>
          <p>We ask what you haul, where you go, how you run. Then we find coverage that actually fits. Not maximum coverage. Right coverage.</p>
        </div>

        <div class="value-col">
          <h3>When You Call, Things Happen</h3>
          <p>You say what you need, it happens. Certificate requests get done while you're on the phone. One moment... done.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Process - horizontal -->
  <section class="process">
    <div class="container">
      <div class="process-header">
        <span class="section-num">02</span>
        <h2>How It Works</h2>
      </div>

      <div class="process-steps">
        <div class="step">
          <span class="step-num">01</span>
          <div class="step-content">
            <h4>Tell Us About Your Operation</h4>
            <p>Quick call or form. We ask the right questions.</p>
          </div>
        </div>
        <div class="step-arrow"></div>
        <div class="step">
          <span class="step-num">02</span>
          <div class="step-content">
            <h4>Get Options That Fit</h4>
            <p>We explain what you're actually buying.</p>
          </div>
        </div>
        <div class="step-arrow"></div>
        <div class="step">
          <span class="step-num">03</span>
          <div class="step-content">
            <h4>Stay Covered</h4>
            <p>Certs when you need them. Answers when you call.</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Full-bleed CTA -->
  <section class="cta">
    <div class="container">
      <div class="cta-content">
        <p class="cta-label">Ready?</p>
        <a href="tel:208-800-0640" class="cta-phone">208-800-0640</a>
      </div>
    </div>
  </section>

</Layout>

<style>
  /* BOLD HERO */
  .hero {
    min-height: 100vh;
    position: relative;
    display: flex;
    align-items: flex-end;
    padding-bottom: 4rem;
    overflow: hidden;
  }

  .hero-bg {
    position: absolute;
    inset: 0;
    z-index: 0;
  }

  .hero-gradient {
    position: absolute;
    inset: 0;
    background: linear-gradient(180deg, #0D2118 0%, #1B4332 100%);
  }

  .hero-grid-pattern {
    position: absolute;
    inset: 0;
    background-image:
      linear-gradient(rgba(255,255,255,0.03) 1px, transparent 1px),
      linear-gradient(90deg, rgba(255,255,255,0.03) 1px, transparent 1px);
    background-size: 80px 80px;
  }

  .hero-content {
    position: relative;
    z-index: 1;
    width: 100%;
    color: white;
  }

  .hero h1 {
    font-size: clamp(4rem, 12vw, 12rem);
    line-height: 0.9;
    letter-spacing: -0.03em;
    margin-bottom: 3rem;
  }

  .line {
    display: block;
  }

  .gold { color: var(--gold); }

  .hero-bottom {
    display: grid;
    grid-template-columns: 1fr auto auto;
    gap: 4rem;
    align-items: end;
  }

  .hero-tagline {
    font-size: 1.25rem;
    line-height: 1.6;
    opacity: 0.8;
    max-width: 300px;
  }

  .btn-primary {
    display: flex;
    flex-direction: column;
    background: var(--gold);
    color: var(--forest);
    padding: 1.5rem 2.5rem;
    border-radius: 4px;
    text-decoration: none;
    text-align: center;
    transition: all 0.3s ease;
  }

  .btn-primary:hover {
    background: white;
    transform: translateY(-4px);
  }

  .btn-label {
    font-size: 0.75rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    opacity: 0.8;
  }

  .btn-number {
    font-family: 'Crimson Pro', serif;
    font-size: 1.5rem;
    font-weight: 600;
  }

  .hero-stats {
    display: flex;
    gap: 2rem;
  }

  .stat {
    text-align: right;
  }

  .stat-value {
    display: block;
    font-family: 'Crimson Pro', serif;
    font-size: 1.5rem;
    font-weight: 600;
    color: var(--gold);
  }

  .stat-name {
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    opacity: 0.6;
  }

  /* VALUES */
  .values {
    padding: 8rem 0;
    background: var(--forest-deep);
    color: white;
  }

  .values-header {
    margin-bottom: 4rem;
    display: flex;
    gap: 2rem;
    align-items: flex-start;
  }

  .section-num {
    font-family: 'Crimson Pro', serif;
    font-size: 1rem;
    color: var(--gold);
    border: 1px solid var(--gold);
    padding: 0.25rem 0.5rem;
    border-radius: 2px;
    flex-shrink: 0;
  }

  .values h2 {
    font-size: clamp(2rem, 4vw, 3.5rem);
    line-height: 1.1;
  }

  .value-columns {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 3rem;
    padding-left: 4rem;
  }

  .value-col h3 {
    font-family: 'Crimson Pro', serif;
    font-size: 1.4rem;
    color: var(--gold);
    margin-bottom: 1rem;
  }

  .value-col p {
    color: rgba(255,255,255,0.7);
    line-height: 1.7;
  }

  /* PROCESS */
  .process {
    padding: 6rem 0;
    background: var(--bg-light);
  }

  .process-header {
    display: flex;
    gap: 2rem;
    align-items: center;
    margin-bottom: 4rem;
  }

  .process h2 {
    font-size: 1rem;
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--gray-600);
  }

  .process-steps {
    display: flex;
    align-items: flex-start;
    gap: 2rem;
  }

  .step {
    flex: 1;
    display: flex;
    gap: 1rem;
  }

  .step-num {
    font-family: 'Crimson Pro', serif;
    font-size: 3rem;
    font-weight: 600;
    color: var(--gray-200);
    line-height: 1;
  }

  .step-content h4 {
    font-family: 'Crimson Pro', serif;
    font-size: 1.25rem;
    color: var(--forest);
    margin-bottom: 0.5rem;
  }

  .step-content p {
    color: var(--gray-600);
    font-size: 0.95rem;
  }

  .step-arrow {
    width: 40px;
    height: 2px;
    background: var(--gray-200);
    margin-top: 1.5rem;
    flex-shrink: 0;
  }

  /* CTA */
  .cta {
    padding: 8rem 0;
    background: var(--gold);
  }

  .cta-content {
    text-align: center;
  }

  .cta-label {
    font-size: 1rem;
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 0.15em;
    color: var(--forest);
    opacity: 0.7;
    margin-bottom: 1rem;
  }

  .cta-phone {
    font-family: 'Crimson Pro', serif;
    font-size: clamp(4rem, 10vw, 8rem);
    font-weight: 600;
    color: var(--forest);
    text-decoration: none;
    display: block;
    transition: transform 0.3s ease;
  }

  .cta-phone:hover {
    transform: scale(1.02);
    color: white;
  }

  /* RESPONSIVE */
  @media (max-width: 968px) {
    .hero-bottom {
      grid-template-columns: 1fr;
      gap: 2rem;
    }

    .hero-stats {
      justify-content: flex-start;
    }

    .stat {
      text-align: left;
    }

    .value-columns {
      grid-template-columns: 1fr;
      padding-left: 0;
    }

    .process-steps {
      flex-direction: column;
    }

    .step-arrow {
      width: 2px;
      height: 30px;
      margin-left: 1.5rem;
    }
  }

  @media (max-width: 640px) {
    .hero h1 {
      font-size: 3.5rem;
    }

    .values-header {
      flex-direction: column;
      gap: 1rem;
    }
  }
</style>
