---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Trucking Insurance | RMS Insurance - Finally, Someone on Your Side">

  <!-- Hero Section -->
  <section class="hero">
    <!-- Background elements -->
    <div class="hero-bg">
      <div class="hero-gradient"></div>
      <div class="hero-lines"></div>
      <div class="hero-glow"></div>
    </div>

    <div class="container hero-content">
      <div class="hero-text">
        <div class="hero-badge animate-fade-in-up">
          <span class="badge-dot"></span>
          Serving Owner-Operators Nationwide
        </div>

        <h1 class="animate-fade-in-up delay-1">
          Finally, someone<br/>
          <span class="hero-accent">on your side.</span>
        </h1>

        <p class="hero-sub animate-fade-in-up delay-2">
          Trucking insurance from experts who actually explain things.<br/>
          No jargon. No runaround. Just answers.
        </p>

        <div class="hero-cta animate-fade-in-up delay-3">
          <a href="tel:208-800-0640" class="btn">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor">
              <path d="M6.62 10.79c1.44 2.83 3.76 5.14 6.59 6.59l2.2-2.2c.27-.27.67-.36 1.02-.24 1.12.37 2.33.57 3.57.57.55 0 1 .45 1 1V20c0 .55-.45 1-1 1-9.39 0-17-7.61-17-17 0-.55.45-1 1-1h3.5c.55 0 1 .45 1 1 0 1.25.2 2.45.57 3.57.11.35.03.74-.25 1.02l-2.2 2.2z"/>
            </svg>
            Get My Quote: 208-800-0640
          </a>
          <a href="/new-authorities" class="btn btn-outline">
            New Authority? Start Here
          </a>
        </div>
      </div>

      <!-- Stats floating card -->
      <div class="hero-stats animate-fade-in-up delay-4">
        <div class="stat-item">
          <div class="stat-number">2,000+</div>
          <div class="stat-label">Truckers Helped</div>
        </div>
        <div class="stat-divider"></div>
        <div class="stat-item">
          <div class="stat-number">$14M+</div>
          <div class="stat-label">Coverage Placed</div>
        </div>
        <div class="stat-divider"></div>
        <div class="stat-item">
          <div class="stat-number">2022</div>
          <div class="stat-label">Est.</div>
        </div>
      </div>
    </div>

    <!-- Scroll indicator -->
    <div class="scroll-indicator animate-fade-in delay-5">
      <div class="scroll-line"></div>
    </div>
  </section>

  <!-- Value Props -->
  <section class="value-section">
    <div class="container">
      <div class="section-header">
        <span class="section-tag">Why RMS</span>
        <h2>The Industry Profits From<br/><span class="text-gold">Your Confusion. We Don't.</span></h2>
      </div>

      <div class="value-grid">
        <div class="value-card">
          <div class="value-icon">
            <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/>
            </svg>
          </div>
          <h3>We Actually Explain Things</h3>
          <p>Insurance should make sense to you. If your agent can't explain it clearly, they either don't understand it or don't want you to.</p>
          <div class="value-accent"></div>
        </div>

        <div class="value-card">
          <div class="value-icon">
            <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/>
            </svg>
          </div>
          <h3>We Know Your Business First</h3>
          <p>We ask what you haul, where you go, how you run. Then we find coverage that actually fits. Not maximum coverage. Right coverage.</p>
          <div class="value-accent"></div>
        </div>

        <div class="value-card">
          <div class="value-icon">
            <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <circle cx="12" cy="12" r="10"/>
              <polyline points="12,6 12,12 16,14"/>
            </svg>
          </div>
          <h3>When You Call, Things Happen</h3>
          <p>You say what you need, it happens. Certificate requests get done while you're on the phone. One moment... done.</p>
          <div class="value-accent"></div>
        </div>
      </div>
    </div>
  </section>

  <!-- How It Works -->
  <section class="process-section">
    <div class="container">
      <div class="section-header">
        <span class="section-tag">The Process</span>
        <h2>Three Steps to<br/><span class="text-gold">Staying Covered</span></h2>
      </div>

      <div class="process-grid">
        <div class="process-step">
          <div class="step-number">01</div>
          <div class="step-content">
            <h3>Tell Us About Your Operation</h3>
            <p>Quick call or form. We ask the right questions so you don't have to guess what we need.</p>
          </div>
          <div class="step-line"></div>
        </div>

        <div class="process-step">
          <div class="step-number">02</div>
          <div class="step-content">
            <h3>Get Options That Fit</h3>
            <p>We shop the same carriers everyone uses, but we explain what you're actually buying &mdash; no insurance jargon.</p>
          </div>
          <div class="step-line"></div>
        </div>

        <div class="process-step">
          <div class="step-number">03</div>
          <div class="step-content">
            <h3>Stay Covered, Stay Moving</h3>
            <p>Certs when you need them. Answers when you have questions. We're here.</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- FAQ -->
  <section class="faq-section">
    <div class="container">
      <div class="faq-layout">
        <div class="faq-header">
          <span class="section-tag">FAQ</span>
          <h2>Common<br/><span class="text-gold">Questions</span></h2>
          <p class="faq-intro">Straight answers. No runaround.</p>
        </div>

        <div class="faq-list">
          <details class="faq-item">
            <summary>
              <span>Do I really need all this coverage?</span>
              <div class="faq-icon">
                <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                  <line x1="12" y1="5" x2="12" y2="19"/>
                  <line x1="5" y1="12" x2="19" y2="12"/>
                </svg>
              </div>
            </summary>
            <div class="faq-answer">
              <p>Some coverage is required by law (liability, cargo for certain loads). Some is smart protection. We'll tell you the difference and let you decide.</p>
            </div>
          </details>

          <details class="faq-item">
            <summary>
              <span>How fast can I get a quote?</span>
              <div class="faq-icon">
                <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                  <line x1="12" y1="5" x2="12" y2="19"/>
                  <line x1="5" y1="12" x2="19" y2="12"/>
                </svg>
              </div>
            </summary>
            <div class="faq-answer">
              <p>Most quotes same day. If you have your info ready, often within a couple hours.</p>
            </div>
          </details>

          <details class="faq-item">
            <summary>
              <span>Is your price lower than other agencies?</span>
              <div class="faq-icon">
                <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                  <line x1="12" y1="5" x2="12" y2="19"/>
                  <line x1="5" y1="12" x2="19" y2="12"/>
                </svg>
              </div>
            </summary>
            <div class="faq-answer">
              <p>Honest answer: probably not. We all use the same carriers for new authorities. The price is basically the same everywhere. The difference is who picks up when you call, and whether they actually know the answer. <a href="/blog/pricing-truth">Read more about how trucking insurance pricing works.</a></p>
            </div>
          </details>

          <details class="faq-item">
            <summary>
              <span>I just got my authority. Where do I start?</span>
              <div class="faq-icon">
                <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                  <line x1="12" y1="5" x2="12" y2="19"/>
                  <line x1="5" y1="12" x2="19" y2="12"/>
                </svg>
              </div>
            </summary>
            <div class="faq-answer">
              <p>Right here. <a href="/new-authorities">See our guide for new authorities.</a></p>
            </div>
          </details>
        </div>
      </div>
    </div>
  </section>

  <!-- Final CTA -->
  <section class="cta-section">
    <div class="cta-bg">
      <div class="cta-gradient"></div>
    </div>
    <div class="container cta-content">
      <h2>Ready to Talk?</h2>
      <p>Call us. We answer.</p>
      <a href="tel:208-800-0640" class="btn btn-large">
        <svg width="24" height="24" viewBox="0 0 24 24" fill="currentColor">
          <path d="M6.62 10.79c1.44 2.83 3.76 5.14 6.59 6.59l2.2-2.2c.27-.27.67-.36 1.02-.24 1.12.37 2.33.57 3.57.57.55 0 1 .45 1 1V20c0 .55-.45 1-1 1-9.39 0-17-7.61-17-17 0-.55.45-1 1-1h3.5c.55 0 1 .45 1 1 0 1.25.2 2.45.57 3.57.11.35.03.74-.25 1.02l-2.2 2.2z"/>
        </svg>
        208-800-0640
      </a>
    </div>
  </section>

</Layout>

<style>
  /* ========================================
     HERO SECTION
     ======================================== */
  .hero {
    position: relative;
    min-height: 100vh;
    display: flex;
    align-items: center;
    overflow: hidden;
    padding: 2rem 0;
  }

  .hero-bg {
    position: absolute;
    inset: 0;
    z-index: 0;
  }

  .hero-gradient {
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg, #1B4332 0%, #0D2118 50%, #0A1A12 100%);
  }

  .hero-lines {
    position: absolute;
    inset: 0;
    background-image:
      linear-gradient(90deg, rgba(255,255,255,0.03) 1px, transparent 1px),
      linear-gradient(rgba(255,255,255,0.03) 1px, transparent 1px);
    background-size: 60px 60px;
  }

  .hero-glow {
    position: absolute;
    top: -20%;
    right: -10%;
    width: 60%;
    height: 80%;
    background: radial-gradient(ellipse, rgba(218, 165, 32, 0.12) 0%, transparent 70%);
    filter: blur(60px);
  }

  .hero-content {
    position: relative;
    z-index: 1;
    color: white;
  }

  .hero-text {
    max-width: 800px;
  }

  .hero-badge {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    background: rgba(218, 165, 32, 0.15);
    border: 1px solid rgba(218, 165, 32, 0.3);
    padding: 8px 16px;
    border-radius: 100px;
    font-size: 0.85rem;
    font-weight: 500;
    letter-spacing: 0.05em;
    margin-bottom: 2rem;
  }

  .badge-dot {
    width: 8px;
    height: 8px;
    background: var(--gold);
    border-radius: 50%;
    animation: pulse 2s infinite;
  }

  .hero h1 {
    font-size: clamp(3rem, 8vw, 5rem);
    line-height: 1.1;
    margin-bottom: 1.5rem;
  }

  .hero-accent {
    color: var(--gold);
  }

  .hero-sub {
    font-size: 1.25rem;
    line-height: 1.7;
    opacity: 0.85;
    margin-bottom: 2.5rem;
    max-width: 520px;
  }

  .hero-cta {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
  }

  .hero-stats {
    display: inline-flex;
    align-items: center;
    gap: 2rem;
    background: rgba(255, 255, 255, 0.05);
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.1);
    padding: 1.5rem 2.5rem;
    border-radius: 8px;
    margin-top: 4rem;
  }

  .stat-item {
    text-align: center;
  }

  .stat-number {
    font-family: 'Crimson Pro', Georgia, serif;
    font-size: 2.5rem;
    font-weight: 600;
    color: var(--gold);
  }

  .stat-label {
    font-size: 0.85rem;
    opacity: 0.7;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .stat-divider {
    width: 1px;
    height: 40px;
    background: rgba(255, 255, 255, 0.2);
  }

  .scroll-indicator {
    position: absolute;
    bottom: 2rem;
    left: 50%;
    transform: translateX(-50%);
  }

  .scroll-line {
    width: 2px;
    height: 60px;
    background: linear-gradient(to bottom, var(--gold), transparent);
    animation: scrollPulse 2s infinite;
  }

  @keyframes scrollPulse {
    0%, 100% { opacity: 1; transform: scaleY(1); }
    50% { opacity: 0.5; transform: scaleY(0.8); }
  }

  /* ========================================
     VALUE SECTION
     ======================================== */
  .value-section {
    padding: 8rem 0;
    background: var(--bg-light);
    position: relative;
  }

  .section-header {
    margin-bottom: 4rem;
  }

  .section-tag {
    display: inline-block;
    font-family: 'Crimson Pro', Georgia, serif;
    font-size: 0.95rem;
    font-weight: 600;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--gold);
    margin-bottom: 1rem;
  }

  .section-header h2 {
    font-size: clamp(2.2rem, 5vw, 3.5rem);
    color: var(--forest);
  }

  .text-gold {
    color: var(--gold);
  }

  .value-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
    gap: 2rem;
  }

  .value-card {
    background: white;
    padding: 2.5rem;
    border-radius: 8px;
    position: relative;
    overflow: hidden;
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    border: 1px solid var(--gray-200);
  }

  .value-card:hover {
    transform: translateY(-8px);
    box-shadow: 0 20px 40px rgba(27, 67, 50, 0.1);
  }

  .value-card:hover .value-accent {
    transform: scaleX(1);
  }

  .value-icon {
    width: 60px;
    height: 60px;
    background: linear-gradient(135deg, var(--forest) 0%, var(--forest-deep) 100%);
    border-radius: 8px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    margin-bottom: 1.5rem;
  }

  .value-card h3 {
    font-family: 'Crimson Pro', Georgia, serif;
    font-size: 1.5rem;
    font-weight: 600;
    color: var(--forest);
    margin-bottom: 1rem;
  }

  .value-card p {
    color: var(--gray-600);
    line-height: 1.7;
  }

  .value-accent {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    height: 4px;
    background: var(--gold);
    transform: scaleX(0);
    transform-origin: left;
    transition: transform 0.4s ease;
  }

  /* ========================================
     PROCESS SECTION
     ======================================== */
  .process-section {
    padding: 8rem 0;
    background: white;
  }

  .process-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 0;
  }

  .process-step {
    position: relative;
    padding: 2rem;
    text-align: center;
  }

  .step-number {
    font-family: 'Crimson Pro', Georgia, serif;
    font-size: 5rem;
    font-weight: 600;
    color: var(--gray-100);
    line-height: 1;
    margin-bottom: 1rem;
    transition: color 0.3s ease;
  }

  .process-step:hover .step-number {
    color: var(--gold);
  }

  .step-content h3 {
    font-family: 'Crimson Pro', Georgia, serif;
    font-size: 1.4rem;
    font-weight: 600;
    color: var(--forest);
    margin-bottom: 0.75rem;
  }

  .step-content p {
    color: var(--gray-600);
    line-height: 1.7;
    max-width: 280px;
    margin: 0 auto;
  }

  .step-line {
    position: absolute;
    top: 50%;
    right: 0;
    width: 100%;
    height: 2px;
    background: linear-gradient(90deg, transparent, var(--gray-200), transparent);
    transform: translateY(-50%);
    z-index: 0;
  }

  .process-step:last-child .step-line {
    display: none;
  }

  /* ========================================
     FAQ SECTION
     ======================================== */
  .faq-section {
    padding: 8rem 0;
    background: var(--forest);
    color: white;
  }

  .faq-layout {
    display: grid;
    grid-template-columns: 1fr 2fr;
    gap: 4rem;
    align-items: start;
  }

  .faq-header h2 {
    font-size: clamp(2.2rem, 5vw, 3rem);
    margin-bottom: 1rem;
  }

  .faq-intro {
    opacity: 0.7;
    font-size: 1.1rem;
  }

  .faq-list {
    display: flex;
    flex-direction: column;
    gap: 1px;
    background: rgba(255, 255, 255, 0.1);
    border-radius: 8px;
    overflow: hidden;
  }

  .faq-item {
    background: rgba(255, 255, 255, 0.05);
  }

  .faq-item summary {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1.5rem 2rem;
    cursor: pointer;
    font-weight: 500;
    font-size: 1.1rem;
    list-style: none;
    transition: background 0.3s ease;
  }

  .faq-item summary::-webkit-details-marker {
    display: none;
  }

  .faq-item summary:hover {
    background: rgba(255, 255, 255, 0.08);
  }

  .faq-icon {
    flex-shrink: 0;
    transition: transform 0.3s ease;
  }

  .faq-item[open] .faq-icon {
    transform: rotate(45deg);
  }

  .faq-answer {
    padding: 0 2rem 1.5rem;
    color: rgba(255, 255, 255, 0.7);
    line-height: 1.7;
  }

  .faq-answer a {
    color: var(--gold);
  }

  /* ========================================
     CTA SECTION
     ======================================== */
  .cta-section {
    position: relative;
    padding: 8rem 0;
    overflow: hidden;
  }

  .cta-bg {
    position: absolute;
    inset: 0;
  }

  .cta-gradient {
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg, var(--gold) 0%, #C49B1A 100%);
  }

  .cta-content {
    position: relative;
    z-index: 1;
    text-align: center;
    color: white;
  }

  .cta-content h2 {
    font-size: clamp(2.5rem, 6vw, 4rem);
    margin-bottom: 1rem;
  }

  .cta-content p {
    font-size: 1.5rem;
    opacity: 0.9;
    margin-bottom: 2.5rem;
  }

  .btn-large {
    font-size: 1.5rem;
    padding: 20px 48px;
    background: white;
    color: var(--forest);
  }

  .btn-large:hover {
    background: var(--forest);
    color: white;
    transform: translateY(-4px);
    box-shadow: 0 12px 32px rgba(0, 0, 0, 0.3);
  }

  /* ========================================
     RESPONSIVE
     ======================================== */
  @media (max-width: 968px) {
    .process-grid {
      grid-template-columns: 1fr;
      gap: 2rem;
    }

    .step-line {
      display: none;
    }

    .faq-layout {
      grid-template-columns: 1fr;
      gap: 2rem;
    }

    .hero-stats {
      flex-wrap: wrap;
      justify-content: center;
    }

    .stat-divider {
      display: none;
    }
  }

  @media (max-width: 640px) {
    .hero {
      padding-top: 4rem;
    }

    .hero-cta {
      flex-direction: column;
    }

    .hero-cta .btn {
      width: 100%;
      justify-content: center;
    }

    .value-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
