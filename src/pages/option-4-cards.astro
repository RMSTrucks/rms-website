---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Option 4: Card-Based | RMS Insurance" noindex={true}>

  <!-- OPTION 4: CARD-BASED - Hero with floating cards, visual structure -->
  <section class="hero">
    <div class="hero-bg">
      <div class="hero-gradient"></div>
    </div>

    <div class="container">
      <div class="hero-layout">
        <!-- Main content card -->
        <div class="main-card">
          <span class="badge">Trucking Insurance</span>
          <h1>Finally, someone<br/><span class="gold">on your side.</span></h1>
          <p>Trucking insurance from experts who actually explain things. No jargon. No runaround. Just answers.</p>

          <div class="cta-row">
            <a href="tel:208-800-0640" class="btn">
              <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor">
                <path d="M6.62 10.79c1.44 2.83 3.76 5.14 6.59 6.59l2.2-2.2c.27-.27.67-.36 1.02-.24 1.12.37 2.33.57 3.57.57.55 0 1 .45 1 1V20c0 .55-.45 1-1 1-9.39 0-17-7.61-17-17 0-.55.45-1 1-1h3.5c.55 0 1 .45 1 1 0 1.25.2 2.45.57 3.57.11.35.03.74-.25 1.02l-2.2 2.2z"/>
              </svg>
              208-800-0640
            </a>
            <a href="/new-authorities" class="link-cta">New Authority? Start Here &rarr;</a>
          </div>
        </div>

        <!-- Floating stat cards -->
        <div class="stat-cards">
          <div class="stat-card card-1">
            <span class="stat-icon">
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/>
                <circle cx="9" cy="7" r="4"/>
                <path d="M23 21v-2a4 4 0 0 0-3-3.87"/>
                <path d="M16 3.13a4 4 0 0 1 0 7.75"/>
              </svg>
            </span>
            <span class="stat-num">2,000+</span>
            <span class="stat-label">Truckers Helped</span>
          </div>

          <div class="stat-card card-2">
            <span class="stat-icon">
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/>
              </svg>
            </span>
            <span class="stat-num">$14M+</span>
            <span class="stat-label">Coverage Placed</span>
          </div>

          <div class="stat-card card-3">
            <span class="stat-icon">
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <circle cx="12" cy="12" r="10"/>
                <polyline points="12,6 12,12 16,14"/>
              </svg>
            </span>
            <span class="stat-num">Since 2022</span>
            <span class="stat-label">Idaho Based</span>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Value cards section -->
  <section class="values">
    <div class="container">
      <div class="section-header">
        <h2>The Industry Profits From Your Confusion.<br/><span class="gold">We Don't.</span></h2>
      </div>

      <div class="value-cards">
        <article class="value-card">
          <div class="card-header">
            <span class="card-num">01</span>
            <div class="card-icon">
              <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/>
              </svg>
            </div>
          </div>
          <h3>We Actually Explain Things</h3>
          <p>Insurance should make sense to you. If your agent can't explain it clearly, they either don't understand it or don't want you to.</p>
        </article>

        <article class="value-card">
          <div class="card-header">
            <span class="card-num">02</span>
            <div class="card-icon">
              <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/>
              </svg>
            </div>
          </div>
          <h3>We Know Your Business First</h3>
          <p>We ask what you haul, where you go, how you run. Then we find coverage that actually fits.</p>
        </article>

        <article class="value-card">
          <div class="card-header">
            <span class="card-num">03</span>
            <div class="card-icon">
              <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <circle cx="12" cy="12" r="10"/>
                <polyline points="12,6 12,12 16,14"/>
              </svg>
            </div>
          </div>
          <h3>When You Call, Things Happen</h3>
          <p>You say what you need, it happens. Certificate requests get done while you're on the phone.</p>
        </article>
      </div>
    </div>
  </section>

  <!-- Process cards -->
  <section class="process">
    <div class="container">
      <h2>How It Works</h2>

      <div class="process-cards">
        <div class="process-card">
          <div class="process-num">1</div>
          <h4>Tell Us About Your Operation</h4>
          <p>Quick call or form. We ask the right questions so you don't have to guess what we need.</p>
        </div>

        <div class="process-connector"></div>

        <div class="process-card">
          <div class="process-num">2</div>
          <h4>Get Options That Fit</h4>
          <p>We shop the same carriers everyone uses, but we explain what you're actually buying.</p>
        </div>

        <div class="process-connector"></div>

        <div class="process-card">
          <div class="process-num">3</div>
          <h4>Stay Covered, Stay Moving</h4>
          <p>Certs when you need them. Answers when you have questions. We're here.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA Card -->
  <section class="cta">
    <div class="container">
      <div class="cta-card">
        <h2>Ready to Talk?</h2>
        <p>Call us. We answer.</p>
        <a href="tel:208-800-0640" class="btn btn-large">
          <svg width="24" height="24" viewBox="0 0 24 24" fill="currentColor">
            <path d="M6.62 10.79c1.44 2.83 3.76 5.14 6.59 6.59l2.2-2.2c.27-.27.67-.36 1.02-.24 1.12.37 2.33.57 3.57.57.55 0 1 .45 1 1V20c0 .55-.45 1-1 1-9.39 0-17-7.61-17-17 0-.55.45-1 1-1h3.5c.55 0 1 .45 1 1 0 1.25.2 2.45.57 3.57.11.35.03.74-.25 1.02l-2.2 2.2z"/>
          </svg>
          208-800-0640
        </a>
      </div>
    </div>
  </section>

</Layout>

<style>
  /* HERO */
  .hero {
    min-height: 100vh;
    position: relative;
    display: flex;
    align-items: center;
    padding: 4rem 0;
    overflow: hidden;
  }

  .hero-bg {
    position: absolute;
    inset: 0;
    z-index: 0;
  }

  .hero-gradient {
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg, #1B4332 0%, #0D2118 60%, #0A1A12 100%);
  }

  .hero-layout {
    position: relative;
    z-index: 1;
    display: grid;
    grid-template-columns: 1.2fr 1fr;
    gap: 4rem;
    align-items: center;
  }

  /* Main content card */
  .main-card {
    background: rgba(255,255,255,0.03);
    backdrop-filter: blur(20px);
    border: 1px solid rgba(255,255,255,0.1);
    border-radius: 16px;
    padding: 3rem;
    color: white;
  }

  .badge {
    display: inline-block;
    font-size: 0.75rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--gold);
    background: rgba(218, 165, 32, 0.15);
    padding: 6px 12px;
    border-radius: 4px;
    margin-bottom: 1.5rem;
  }

  .main-card h1 {
    font-size: clamp(2.5rem, 5vw, 4rem);
    line-height: 1.1;
    margin-bottom: 1.5rem;
  }

  .gold { color: var(--gold); }

  .main-card p {
    font-size: 1.15rem;
    opacity: 0.8;
    line-height: 1.7;
    margin-bottom: 2rem;
  }

  .cta-row {
    display: flex;
    align-items: center;
    gap: 2rem;
    flex-wrap: wrap;
  }

  .link-cta {
    color: white;
    opacity: 0.8;
    font-size: 0.95rem;
    transition: all 0.3s ease;
  }

  .link-cta:hover {
    opacity: 1;
    color: var(--gold);
  }

  /* Floating stat cards */
  .stat-cards {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  .stat-card {
    background: rgba(255,255,255,0.05);
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255,255,255,0.1);
    border-radius: 12px;
    padding: 1.5rem 2rem;
    color: white;
    display: flex;
    align-items: center;
    gap: 1rem;
    transition: all 0.3s ease;
  }

  .stat-card:hover {
    transform: translateX(-8px);
    background: rgba(255,255,255,0.08);
  }

  .card-1 { transform: translateX(20px); }
  .card-2 { transform: translateX(0); }
  .card-3 { transform: translateX(40px); }

  .card-1:hover { transform: translateX(12px); }
  .card-3:hover { transform: translateX(32px); }

  .stat-icon {
    width: 48px;
    height: 48px;
    background: rgba(218, 165, 32, 0.15);
    border-radius: 8px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--gold);
  }

  .stat-num {
    font-family: 'Crimson Pro', serif;
    font-size: 1.75rem;
    font-weight: 600;
    color: var(--gold);
  }

  .stat-label {
    font-size: 0.85rem;
    opacity: 0.7;
  }

  /* VALUES */
  .values {
    padding: 8rem 0;
    background: var(--bg-light);
  }

  .section-header {
    text-align: center;
    margin-bottom: 4rem;
  }

  .section-header h2 {
    font-size: clamp(2rem, 4vw, 3rem);
    color: var(--forest);
  }

  .value-cards {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2rem;
  }

  .value-card {
    background: white;
    border-radius: 12px;
    padding: 2rem;
    border: 1px solid var(--gray-200);
    transition: all 0.3s ease;
  }

  .value-card:hover {
    transform: translateY(-8px);
    box-shadow: 0 20px 40px rgba(27, 67, 50, 0.12);
  }

  .card-header {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    margin-bottom: 1.5rem;
  }

  .card-num {
    font-family: 'Crimson Pro', serif;
    font-size: 2.5rem;
    font-weight: 600;
    color: var(--gray-200);
  }

  .card-icon {
    width: 48px;
    height: 48px;
    background: var(--forest);
    border-radius: 8px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
  }

  .value-card h3 {
    font-family: 'Crimson Pro', serif;
    font-size: 1.35rem;
    color: var(--forest);
    margin-bottom: 0.75rem;
  }

  .value-card p {
    color: var(--gray-600);
    line-height: 1.7;
  }

  /* PROCESS */
  .process {
    padding: 6rem 0;
    background: white;
  }

  .process h2 {
    text-align: center;
    font-size: 1rem;
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--gray-600);
    margin-bottom: 3rem;
  }

  .process-cards {
    display: flex;
    align-items: flex-start;
    justify-content: center;
    gap: 1rem;
  }

  .process-card {
    background: var(--bg-light);
    border-radius: 12px;
    padding: 2rem;
    max-width: 280px;
    text-align: center;
  }

  .process-num {
    width: 40px;
    height: 40px;
    background: var(--gold);
    color: white;
    font-family: 'Crimson Pro', serif;
    font-size: 1.25rem;
    font-weight: 600;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0 auto 1rem;
  }

  .process-card h4 {
    font-family: 'Crimson Pro', serif;
    font-size: 1.2rem;
    color: var(--forest);
    margin-bottom: 0.5rem;
  }

  .process-card p {
    color: var(--gray-600);
    font-size: 0.95rem;
    line-height: 1.6;
  }

  .process-connector {
    width: 40px;
    height: 2px;
    background: var(--gray-200);
    margin-top: 3rem;
  }

  /* CTA */
  .cta {
    padding: 6rem 0;
    background: var(--forest);
  }

  .cta-card {
    background: linear-gradient(135deg, var(--gold) 0%, #C49B1A 100%);
    border-radius: 16px;
    padding: 4rem;
    text-align: center;
    color: white;
  }

  .cta-card h2 {
    font-size: clamp(2.5rem, 5vw, 4rem);
    margin-bottom: 0.5rem;
  }

  .cta-card p {
    font-size: 1.5rem;
    opacity: 0.9;
    margin-bottom: 2rem;
  }

  .btn-large {
    font-size: 1.4rem;
    padding: 18px 40px;
    background: white;
    color: var(--forest);
  }

  .btn-large:hover {
    background: var(--forest);
    color: white;
  }

  /* RESPONSIVE */
  @media (max-width: 968px) {
    .hero-layout {
      grid-template-columns: 1fr;
      gap: 3rem;
    }

    .stat-cards {
      flex-direction: row;
      flex-wrap: wrap;
      justify-content: center;
    }

    .stat-card {
      flex: 1 1 200px;
    }

    .card-1, .card-2, .card-3 {
      transform: none;
    }

    .card-1:hover, .card-3:hover {
      transform: translateY(-4px);
    }

    .value-cards {
      grid-template-columns: 1fr;
    }

    .process-cards {
      flex-direction: column;
      align-items: center;
    }

    .process-connector {
      width: 2px;
      height: 30px;
      margin: 0;
    }
  }

  @media (max-width: 640px) {
    .main-card {
      padding: 2rem;
    }

    .cta-row {
      flex-direction: column;
      align-items: stretch;
    }

    .cta-row .btn {
      justify-content: center;
    }

    .link-cta {
      text-align: center;
    }
  }
</style>
